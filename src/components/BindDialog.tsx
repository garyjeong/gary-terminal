/**
 * BindDialog — removed. Process monitoring is now fully automatic via lsof
 * cwd-matching (see useSystemStats.ts detectRepoServers).
 * This file is kept as a stub so legacy imports resolve without error.
 */
export {};
